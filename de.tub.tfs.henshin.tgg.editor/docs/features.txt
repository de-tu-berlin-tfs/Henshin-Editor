New feature list:

- copy/paste: the past operation should be enabled for the containing tree list element. Currently, 
one has to click on the element itself to paste it via context menu

- execute FTrules: layout of correspondence nodes is not created, but should be
- TGG layouter: should also work on correspondence component, which it currently does not
- alignment actions: they are not working yet. They become active after a move command, but have no effect
- graph label should be placed behind the nodes. Currently, a single small node in the top left corner cannot be selected and moved.

====================================
Remaining items on old feature list

- Import of rules
- Rule application: if there are several matches, the user should be able to select one - e.g. by clicking on a node